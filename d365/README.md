# d365/ — belongs here; provenance + upstream-sync note

> Provenance note (.5148, session `claudecode-outsideVSCode(mainClaudeEXE)`,
> 2026-07-05). Operator clarification: *"d365 IS a m365/d365 script for THIS
> repo unless we rely and reuse it somewhere — in which case keep the
> original in its original place and just document where it comes from so we
> can put an auto-upstream in sometime."* So: this folder is correctly
> placed. This note records origin + the reuse/sync contract.

## What is here

`Publish-KritD365SalesApp.ps1` — Business Central `.app` publisher
(BcContainerHelper, falling back to the BC Admin API). **Generated by
SCXCodex** (scx-coder via the Kritical SCX muxing/proxy stack), committed
`a07233c` (2026-07-05, wave `.5208`) as an end-to-end demonstration that the
SCXCodex build chain produces real, AST-validated D365/BC automation. This
is the correct home — it is a product OF this repo's codegen.

## Origin of record

| Field | Value |
|---|---|
| Producer | SCXCodex (scx-coder) — this repo's own code-generation stack |
| Born commit | `a07233c` @ 2026-07-05 08:18 (wave .5208) |
| Validation | PowerShell AST parse: 0 errors. NOT yet run against live BC. |
| Naming caveat | "D365SalesApp" but it publishes to **Business Central**, not D365 Sales |

## Reuse + auto-upstream contract

This file is currently **origin-here, used-here**. IF another repo later
needs to reuse it (e.g. the KRTPax8ToShopifyConnector publish flow, or
Krit.PowerShellToolkit as a shared utility):

1. **Keep the original here** — this stays the canonical source (it is where
   SCXCodex writes).
2. The consumer takes a **documented copy**, its header citing
   `Kritical.SCXCode/d365/Publish-KritD365SalesApp.ps1 @ <sha>` as origin.
3. Wire an **auto-upstream pull** so the copy tracks this origin: a small
   sync step (scheduled or per-build) that re-copies when the origin sha
   changes, preserving the consumer's provenance header. Record each such
   consumer in the table below so the sync targets are known.

### Consumers (auto-sync targets) — none yet

| Consumer repo | Path | Copied at sha | Sync method |
|---|---|---|---|
| _(none)_ | | | |

## Verdict

- **KEEP HERE.** Correct placement per operator.
- Do NOT wire into any live BC deploy without a review pass — the tested
  publish path elsewhere is al-dev MCP `al_publish` + the Pax8 toolkit; this
  is unproven against a live environment.
- When reuse happens, fill the consumers table + add the auto-upstream step;
  never fork silently.
